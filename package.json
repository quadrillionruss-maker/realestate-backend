{
  "name": "re-module",
  "version": "1.0.0",
  "private": true,
  "description": "Real estate sales operations module (Nigeria MVP) — grafted onto FlowDesk",
  "main": "src/routes/index.js",
  "scripts": {
    "test": "node src/test/logic.test.js",
    "smoke": "node src/test/smoke.js",
    "sync": "node scripts/sync-to-flowdesk.js",
    "sync:check": "node scripts/sync-to-flowdesk.js --check"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "express": "^4.18.2",
    "node-cron": "^4.6.0"
  },
  "peerDependencies": {
    "puppeteer": "^22.0.0"
  },
  "peerDependenciesMeta": {
    "puppeteer": {
      "optional": true
    }
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
